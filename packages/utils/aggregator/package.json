{
  "name": "@spicetime/aggregator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "spicetime-aggregate": "./dist/cli.js"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node dist/cli.js",
    "lint": "eslint src --ext .js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf dist .turbo",
    "docs:generate": "jsdoc -c jsdoc.json"
  }
}