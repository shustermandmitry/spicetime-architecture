overwrite: true
schema: "./src/shared/graphql/schema.gql"
generates:
  ./src/shared/graphql/schema.json:
    plugins:
      - "introspection" # Generates the JSON representation of the schema
  ./src/shared/graphql/resolvers.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"